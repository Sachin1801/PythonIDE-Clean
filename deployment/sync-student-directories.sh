#!/bin/sh
# Placeholder script for ECS deployment compatibility
# This script is overridden in docker-compose files but needed for Dockerfile CMD
echo "Skipping student directory sync (handled by init scripts)"
exit 0
